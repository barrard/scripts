{
  "name": "scripts",
  "version": "0.0.1",
  "repository": "",
  "license": "",
  "dependencies": {
    "concurrently": "^3.1.0"
  },
  "scripts": {
    "install": "(cd express && npm install) && (cd react && npm install)",
    "start": "concurrently \"cd express && npm start\" \"cd react && npm start\"",
    "build": "cd react && npm build"
  }
}
